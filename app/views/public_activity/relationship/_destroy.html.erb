<% if activity.trackable %>
  <% user = activity.trackable.followed %>
  <%= raw t ".message", user_name: link_to(user.name, user_path(user)),
    at: activity.updated_at %>
<% else %>
  <%= t ".removed" %>
<% end %>
