<div class="panel-body">
  <div class="list-group">
    <% @favourited_books.each do |book| %>
      <%= link_to book.name, admin_book_path(book), class: "list-group-item" %>
    <% end %>
  </div>
</div>
